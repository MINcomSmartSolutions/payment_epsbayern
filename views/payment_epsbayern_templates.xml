<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Redirect form: submits a GET to the EPS Bayern paypage URL (which already contains ?txnid=...) -->
    <template id="redirect_form">
        <form t-att-action="api_url" method="get"/>
    </template>

</odoo>
